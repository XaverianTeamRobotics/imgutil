import React, { FC, ReactElement } from "react";

interface Props {
  url: string;
}

export const Image: FC<Props> = ({ url }): ReactElement => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};
